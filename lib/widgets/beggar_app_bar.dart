import 'package:flutter/material.dart';

class BeggarAppBar extends AppBar {
  BeggarAppBar({
    super.key,
    super.title,
    super.elevation = 0.0,
    super.backgroundColor = Colors.transparent,
    super.foregroundColor = Colors.black,
  });
}
