import 'package:flutter/material.dart';

class ShareChallengePage extends StatelessWidget {
  const ShareChallengePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(toString()),
      ),
    );
  }
}
